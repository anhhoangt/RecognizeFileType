Request for an HTML File:

If a client requests /index.html, the server will look for index.html in the current directory.
If index.html exists, it will be served with a 200 status code.
If index.html does not exist, a 404 error will be returned.
Request for a JSON File:

If a client requests /data.json, the server will look for data.json in the current directory.
If data.json exists, it will be served with a 200 status code.
If data.json does not exist, a 404 error will be returned.
Request for an Unsupported File Type:

If a client requests /image.png, the server will return a 404 error indicating that the file type is not supported.

# ..

# ..
